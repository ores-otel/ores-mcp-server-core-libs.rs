SPECIFICATION Spec

CONSTANTS
    AuditCapacity = 3
    MaxRevision = 12

CONSTRAINT RevisionBound

INVARIANTS
    TypeOK
    AuditBounded
    AuditStrictlyOrdered
    AuditLatestMatchesRevision
    StoppedIsTerminal

\* Reaching the terminal stopped state is expected. The invariants distinguish
\* that terminal state from an accidental nonterminal deadlock.
CHECK_DEADLOCK FALSE
